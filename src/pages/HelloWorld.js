const HelloWorld = () => {
  // Camera settings
  const fov = 50;  // in degrees
  const aspect = window.innerWidth / window.innerHeight; // width / height
  const near = 0.1;
  const far = 100;
  
  return (
    <>
    </>
  )
}

export default HelloWorld;
